version: "3"
services:
  redis:
    image: redis:alpine
  web:
    build: .
    command: python example/manage.py runserver 0.0.0.0:8000
    ports:
      - '8000:8000'
    volumes:
      - .:/code
    links:
      - redis

